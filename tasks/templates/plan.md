# 任务计划

## 当前状态

结合文件引用，说明代码现在的行为。

## 实施步骤

1. 步骤 1：最小、可独立验证的改动。
2. 步骤 2：下一个可独立验证的改动。
3. 步骤 3：最终文档、测试或收尾更新。

## 文件范围

预计涉及文件：

- `path/to/file`

除非在 `journal.md` 记录范围扩大的原因，否则不要编辑列表外文件。

## 数据 / API / Schema 变更

- 默认无。

如果需要 schema/API 变更，先在这里说明迁移和兼容要求，再开始编辑。

## 测试计划

- 定向：
  - command
- 全量：
  - `pnpm lint`
  - `pnpm test`
  - `pnpm build`

## 回滚计划

说明如何安全回滚本任务。
